# Interactive textbook and containers

The accepted v0.2.0 scope is a beginner-oriented rebuild of the five-step lesson, not seven additional chapters. Follow the app-ui-ux-best-practices skill: predictable navigation, meaningful diagrams, readable code, and no decorative dashboard content.

- [x] Link authored diagrams, source anchors and contextual concepts across five steps.
- [x] Keep experiments inline, preserve location, add a course recap and optional reflection.
- [x] Capture bounded real tensor samples; distinguish illustrations, replay and legacy results.
- [x] Validate assessment evidence against B=2, T=8 and the pinned source.
- [x] Build CPU learning/development images, Compose Watch, backup/import and independent volumes.
- [x] Verify browser, process lifecycle, persistence and native/container CPU paths.
- [ ] Finish CUDA image/hardware validation after host Docker storage recovery.
- [ ] Verify Docker Desktop restart recovery without disrupting unrelated workloads.
- [ ] Publish v0.2.0 through the release PR with source artifacts and container images.

Desktop panels default to 220px / flexible / 300px. Show one question, an interactive diagram, and the relevant source above the fold at 1366×768. Keep detailed prose and references available on demand. Never imply that a sampled tensor is the complete tensor or that random weights understand language.

Daily use resumes with Docker Desktop. Container ports publish only to host loopback. Default sample execution uses CPU; CUDA is an explicit choice. Existing unrelated containers must not be stopped for validation.

Current external blocker: the host C: partition is full and Docker's virtual disk became read-only. Exported database backups remain on E:. Release PR #2 is intentionally held until container GPU evidence and storage recovery are complete; see `docs/VALIDATION.md`.
